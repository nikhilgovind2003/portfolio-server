# ---------- Base Image ----------
FROM node:18-alpine

# ---------- Set working directory ----------
WORKDIR /app

# ---------- Install dependencies ----------
COPY package*.json ./
RUN npm install --production

# ---------- Copy source ----------
COPY . .

# ---------- Expose API port ----------
EXPOSE 5000

# ---------- Start application ----------
CMD ["npm", "start"]
